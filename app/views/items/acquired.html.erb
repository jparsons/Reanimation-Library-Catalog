<% title "Newly acquired items" %>
<h2><%= yield(:title) %></h2>
<% @items.each do |item| %>
  <p><%= item.title %><%= item.subtitle.nil? ? "" : ": #{item.subtitle}" %> &nbsp; <%= link_to "continue cataloging", edit_item_path(item) %></p>
<% end %>